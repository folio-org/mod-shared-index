{
  "description": "Ingest job information",
  "type": "object",
  "properties": {
    "jobId": {
      "type": "string",
      "description" : "job identifier",
      "format": "uuid"
    },
    "numberOfRecords": {
      "type": "integer",
      "description" : "number of records in job so far"
    }
  },
  "additionalProperties": false,
  "required": [
    "jobId"
  ]
}
